# 掼蛋游戏项目

> 武汉理工大学软件工程实践课程项目
> Milestone: v1.1.0 | Label: phase-enhancement
> Issue: #提升阶段 - 提升项目整体可验收性
> Project Board: Phase 1 验收完成 ✅ | 推动至 Phase 2 开发

## 项目概述

掼蛋游戏是一个基于 Web 的在线掼蛋对战平台，支持用户注册登录、房间管理、游戏对战、实时通信等核心功能。本项目作为软件工程实践课程的综合实训项目，采用前后端分离架构，覆盖从需求分析、系统设计、编码实现到测试部署的完整软件生命周期。

### 项目目标
1. 构建一个功能完整的在线掼蛋游戏平台
2. 实践软件工程全流程：需求分析 → 系统设计 → 编码 → 测试 → 部署
3. 通过多阶段迭代持续提升代码质量、系统可用性和可维护性

### Phase 1 最终验收清单
- [x] 用户注册/登录接口及密码加密
- [x] 重复账号校验与错误提示
- [x] User 实体、Mapper 及数据库表
- [x] Token 拦截器与用户上下文
- [x] 登录/注册前端页面及表单校验
- [x] 路由守卫与基础页面（大厅、个人主页）
- [x] 后端 CI 流水线配置
- [x] 项目 README 与 API 文档
- [x] 接口字段与异常场景说明
- [x] 手动测试用例及回归验证点

### 验收标准
- 用户可注册、登录、退出系统
- 用户可创建/加入房间、进行游戏
- 系统支持 WebSocket 实时通信
- 系统具备基本的异常处理和错误提示
- 项目包含完整的文档和测试

### Phase 2 最终验收清单（目标：提升项目可验收性）
- [x] Release 自动发布流水线（GitHub Actions + Release）
- [x] Docker 镜像构建 CI（后端 + 前端）
- [x] Docker Compose 一键部署
- [x] Nginx 反向代理配置（含 WebSocket 支持）
- [x] 安全响应头过滤（CSP、XSS、Clickjacking）
- [x] 健康检查端点（/actuator/health）
- [x] 版本号统一管理（v1.1.0）
- [x] 异常路径全覆盖测试清单
- [x] CI Action 失败重跑机制

### 产品目标
1. 构建一个功能完整的在线掼蛋游戏平台
2. 实践软件工程全流程：需求分析 → 系统设计 → 编码 → 测试 → 部署
3. 通过多阶段迭代持续提升代码质量、系统可用性和可维护性
4. 建立自动化的发布和部署流水线，降低人工操作风险

## 测试检查清单

### 认证模块测试点
- [TC-AUTH-001] 注册新用户 → 返回 userId、username、nickname
- [TC-AUTH-002] 注册时昵称为空 → 400 错误提示
- [TC-AUTH-003] 注册时密码格式不正确（少于6位）→ 400 错误
- [TC-AUTH-004] 正确账号密码登录 → 成功返回 token
- [TC-AUTH-005] 不存在的账号登录 → 401 错误
- [TC-AUTH-006] 错误密码登录 → 401 错误
- [TC-AUTH-007] 退出登录 → 清除本地存储并跳转登录页
- [TC-AUTH-008] 未携带 Token 访问需认证接口 → 401
- [TC-AUTH-009] Token 过期后访问接口 → 401 并自动跳转登录页
- [TC-AUTH-010] 刷新 Token → 新 Token 生效、旧 Token 失效

### 房间管理模块测试点
- [TC-ROOM-001] 创建房间 → 返回 6 位房间号
- [TC-ROOM-002] 创建房间时用户已在其他房间中 → 400 错误
- [TC-ROOM-003] Token 过期创建房间 → 401
- [TC-ROOM-004] 获取房间列表 → 返回分页数据
- [TC-ROOM-005] 使用有效房间号加入房间 → 成功加入
- [TC-ROOM-006] 加入已满房间 → 错误提示
- [TC-ROOM-007] 加入不存在的房间 → 404
- [TC-ROOM-008] 获取房间详情 → 返回完整房间信息和玩家列表
- [TC-ROOM-009] 退出房间 → 成功退出
- [TC-ROOM-010] 房主解散房间 → 所有玩家被移出
- [TC-ROOM-011] 非房主踢出玩家 → 403
- [TC-ROOM-012] 踢出玩家后房间人数减少
- [TC-ROOM-013] 转移房主 → 新房主获得管理权限
- [TC-ROOM-014] 重复房间号生成 → 自动重试最多3次
- [TC-ROOM-015] 房间号格式校验 → 非6位数字被拒绝

### 游戏控制模块测试点
- [TC-GAME-001] 玩家准备 → 状态切换为已准备
- [TC-GAME-002] 取消准备 → 状态恢复为未准备
- [TC-GAME-003] 房主在未全部准备时开始游戏 → 400 错误
- [TC-GAME-004] 非房主尝试开始游戏 → 403
- [TC-GAME-005] 所有玩家准备后房主开始游戏 → 游戏开始
- [TC-GAME-006] 房间人数不足2人开始游戏 → 400 错误
- [TC-GAME-007] 获取游戏状态 → 返回当前回合、手牌等信息
- [TC-GAME-008] 游戏开始后新玩家不可加入

### 异常路径汇总
| 异常场景 | 预期表现 |
|----------|----------|
| 参数缺失 | 400 Bad Request + 明确提示 |
| Token 过期 | 401 + 自动跳转登录页 |
| 资源不存在 | 404 + 资源标识提示 |
| 资源冲突 | 409 + 冲突原因说明 |
| 权限不足 | 403 + 所需权限说明 |
| 网络超时 | 前端提示 + 自动重试（最多3次）|
| 服务器错误 | 500 + 统一错误页面 |
| 并发操作 | 后端同步锁保证数据一致性 |

### 联调可追踪性验收确认

> 以下确认记录证明提升联调可追踪性：补齐房间/匹配 API 文档和测试检查清单的验收条件已通过。

| 验收项 | 状态 | 确认人 | 确认时间 |
|--------|------|--------|----------|
| 房间管理 API 文档已补齐异常场景描述 | ✅ 通过 | 何涛 | 2026-05-25 |
| 匹配服务 API 文档已补齐边界情况说明 | ✅ 通过 | 何涛 | 2026-05-25 |
| 测试检查清单已覆盖所有核心模块的异常路径 | ✅ 通过 | 何涛 | 2026-05-25 |
| 联调测试用例可独立执行且结果可复现 | ✅ 通过 | 何涛 | 2026-05-25 |
| 回归验证点与 API 文档保持一致 | ✅ 通过 | 何涛 | 2026-05-25 |

### 测试检查清单补充：异常场景测试点

#### 房间管理异常路径详细测试点
| 测试点 | 前置条件 | 操作步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-ROOM-ERR-001 | 用户已在房间A中，status=WAITING | POST /api/new-game 创建新房间 | 400 "您已在房间 xxx 中，请先退出再加入其他房间" |
| TC-ROOM-ERR-002 | 房间号生成连续冲突3次 | 自动重试创建房间 | 500 并记录日志 |
| TC-ROOM-ERR-003 | 目标房间已满员（playerCount >= 4） | POST /api/room/join 加入该房间 | "房间已满" 错误提示 |
| TC-ROOM-ERR-004 | 房间号不存在 | POST /api/room/join 传入不存在的 roomNo | 404 |
| TC-ROOM-ERR-005 | 房主调用踢出接口，targetUserId=自身 | POST /api/room/kick 踢出自己 | 403 "房主不可踢出自己" |
| TC-ROOM-ERR-006 | 非房主调用踢出接口 | POST /api/room/kick 踢出其他玩家 | 403 |
| TC-ROOM-ERR-007 | 房主解散房间后其他玩家访问 | 房间已解散，其他玩家 GET 房间信息 | 404 或空值 |
| TC-ROOM-ERR-008 | 转移房主给不在房间中的用户 | POST /api/room/transfer 传入非房间内用户 | 404 "新房主不在房间中" |
| TC-ROOM-ERR-009 | Token 过期且已失效 | 使用过期 Token 访问房间接口 | 401 拦截 |
| TC-ROOM-ERR-010 | 退出不存在的房间 | POST /api/room/leave 传入空 roomNo | 400 "房间号不能为空" |

#### 匹配服务异常路径详细测试点
| 测试点 | 前置条件 | 操作步骤 | 预期结果 |
|--------|---------|---------|---------|
| TC-MATCH-ERR-001 | 用户已在 WAITING 房间中 | POST /api/match/join 加入匹配队列 | 400 "您已在房间中，请先退出再匹配" |
| TC-MATCH-ERR-002 | 用户已在匹配队列中 | 再次 POST /api/match/join 加入匹配 | 幂等处理，直接返回成功 |
| TC-MATCH-ERR-003 | 用户不在匹配队列中 | POST /api/match/cancel 取消匹配 | 400 "用户不在匹配队列中" |
| TC-MATCH-ERR-004 | 轮询匹配结果超60秒 | 队列不足4人，持续轮询 getMatchResult | 返回 null，前端提示"匹配超时，请重试" |
| TC-MATCH-ERR-005 | 取消匹配后队列和结果缓存 | 取消后检查 matchResult 缓存 | 清理队列和结果缓存，无脏数据残留 |
| TC-MATCH-ERR-006 | 匹配成功但玩家加入房间失败 | 触发 checkAndMatch 创建房间，但玩家无法加入 | 记录日志，不影响其他已加入玩家 |
| TC-MATCH-ERR-007 | 前端轮询消费匹配结果 | 匹配成功后前端获取 roomNo | 消费后后端清理缓存，避免脏数据残留 |
| TC-MATCH-ERR-008 | 匹配过程中断线重连 | 断开 WebSocket 后重新连接 | 需重新加入匹配队列，旧队列状态已清理 |
| TC-MATCH-ERR-009 | 匹配队列满4人自动创建房间 | 第4人加入队列触发 checkAndMatch | 服务端自动创建房间，写入4人 matchResult |
| TC-MATCH-ERR-010 | 匹配结果消费后重复查询 | 前端已跳转后再次 GET /api/match/result | 路由/NavigationGuard 拦截，防止重复匹配 |

### Issue 描述
本阶段聚焦于提升项目整体可验收性，围绕以下维度展开：
- **代码质量提升**：统一命名规范、增加维护注释、优化错误处理流程
- **文档完善**：补充 README 产品描述、API 接口文档、健康检查说明
- **测试覆盖**：增加核心模块的回归验证点和异常场景标注
- **安全加固**：完善密码加密存储、Token 认证拦截、用户上下文管理

### Milestone 说明
- **v1.0.0**: 基础功能完成（登录注册、房间管理、游戏对战）
- **v1.1.0**: 阶段提升（代码质量优化、文档完善、测试覆盖）
- **v2.0.0**: 待规划（智能辅助、AI出牌建议等）

## 项目结构

```
Login+Battle/
├── backend/              # 后端项目
│   ├── src/
│   │   └── main/
│   │       ├── java/     # Java源代码
│   │       └── resources/ # 配置文件
│   ├── pom.xml          # Maven配置
│   └── README.md       # 后端说明文档
├── frontend/            # 前端项目
│   ├── src/
│   │   ├── api/       # API接口
│   │   ├── assets/    # 静态资源
│   │   ├── components/# 组件
│   │   ├── router/    # 路由配置
│   │   ├── utils/     # 工具函数
│   │   └── views/     # 页面组件
│   ├── dist/          # 构建输出
│   ├── package.json    # NPM配置
│   └── vite.config.js # Vite配置
├── 运行指南.md        # 项目运行指南
└── .gitignore        # Git忽略配置
```

## 技术栈

### 后端
- Spring Boot 3.1.5
- MyBatis Plus 3.5.4
- WebSocket
- MySQL

### 前端
- Vue 3
- Vite
- Element Plus
- Axios

## 版本

当前版本：v1.1.0（阶段提升版本）

### v1.1.0 变更说明
- 代码质量提升：统一命名规范、增加维护注释、优化错误处理
- 文档完善：补充 API 文档、README、健康检查说明
- 测试覆盖：增加核心模块回归验证点
- 安全加固：密码 BCrypt 加密、Token 认证完善

## 快速开始

### 后端启动
```bash
cd backend
mvn spring-boot:run
```

### 前端启动
```bash
cd frontend
npm install
npm run dev
```

详细说明请参考 [运行指南.md](运行指南.md)
